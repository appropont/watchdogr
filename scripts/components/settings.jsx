'use strict';

var React = require('react'),

    Settings = React.createClass({
        render: function() {

            return (
                <div className="header">
                    <h1>Settings</h1>
                </div>
            )
        }
    });

module.exports = Settings;
